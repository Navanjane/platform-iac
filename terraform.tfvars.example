# Terraform Variables Example
# Copy this file to terraform.tfvars and update with your actual values

# Root domain name for the platform (managed in Route53)
domain_name = "plat.devopsforge.xyz"  # Your platform domain

# IAM role assumption (set to false for initial bootstrap only)
use_iam_role = true
